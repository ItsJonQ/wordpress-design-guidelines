/**
 * External dependencies
 */
import styled from 'styled-components';

const Label = styled.div`
	color: var(--colorLightGray600);
	font-size: 0.8rem;
	margin-bottom: 5px;

	&:last-child {
		margin-bottom: 0;
	}
`;

export default Label;
